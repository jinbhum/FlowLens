# FlowLens 활성 앱 추적 신뢰성 개선

## 개선 배경

이전 추적기는 전경 창의 제목을 1초마다 읽어 앱을 식별했습니다. 이 방식은 다음 상황에서 기록 누락 또는 중복을 만들 수 있습니다.

- 제목이 없는 UWP, 게임, 미디어, 보안 창
- 문서·탭·회의 이름처럼 창 제목이 자주 변경되는 앱
- 1초보다 짧게 전경이 된 런처, 파일 선택 창, 인증 창, 알림 창
- 실행 파일 경로를 조회할 권한이 제한된 보호 프로세스

## v0.5.2 추적 방식

v0.5.2는 전경 창을 **200ms 간격**으로 확인합니다. 앱의 집계 키는 창 제목이 아니라 아래 정보를 조합해 만듭니다.

| 역할 | 로컬 수집 정보 | 사용 방법 |
|---|---|---|
| 전환 감지 | 창 핸들(HWND) + 프로세스 ID(PID) | 다른 창이 전경으로 이동했는지 판단 |
| 앱 집계 | 실행 파일명 | 제목이 달라져도 같은 앱으로 시간과 활성화 횟수를 누적 |
| 표시 정보 | 최근 창 제목 | 앱 목록과 타임라인에서 참고용으로만 표시 |
| 접근 제한 대체값 | PID 기반 로컬 라벨 | 실행 파일명을 조회하지 못한 창도 누락하지 않고 기록 |

짧은 활성화는 밀리초 단위로 앱별 `active_millis`에 저장됩니다. 화면에서는 1초 미만의 사용도 `200ms`처럼 표시할 수 있으며, 일일 특징 벡터의 앱 시간 비율에도 포함됩니다.

## 개인정보 보호 경계

실행 파일명은 로컬 앱 식별에만 사용합니다. 창 제목은 앱 목록 및 타임라인의 표시용으로 저장될 수 있지만, 임베딩 입력에는 전달하지 않습니다. 키 입력 내용, 브라우저 URL, 화면 내용, 프로세스 명령줄, 파일 경로, 패킷 내용은 수집하지 않습니다.

프로세스 이미지 이름 조회가 거부되는 경우 FlowLens는 관리자 권한을 올리거나 보호 기능을 우회하지 않습니다. 대신 `보호된 프로세스 (PID nnnn)` 형식의 로컬 대체 라벨을 기록합니다. 이 때문에 보호 프로세스가 정확한 앱 이름으로 표시되지 않을 수 있지만, 활성화 자체가 누락되지는 않습니다.

## 사용자 검증 절차

1. v0.5.2를 설치한 뒤 FlowLens를 완전히 종료하고 다시 실행합니다.
2. 제목이 자주 변하는 앱, 제목이 없는 창, 짧게 열리는 파일 선택 창을 각각 활성화합니다.
3. 앱 목록에서 실행 파일명 기준 항목이 생기는지 확인합니다. 짧은 사용은 `ms` 단위 시간으로 나타날 수 있습니다.
4. 타임라인에서 앱 전환이 기록되는지 확인합니다.
5. 보호된 앱이 정확한 이름으로 표시되지 않더라도 `보호된 프로세스 (PID …)` 항목과 전환 수가 기록되는지 확인합니다.

재설치 뒤에도 특정 앱이 전혀 남지 않는 경우, 해당 앱의 종류와 대략적인 활성화 시점만 알려 주면 됩니다. 창 제목·문서명·업무 내용은 전달할 필요가 없습니다. 이 정보로 Win32 전경 창 조회가 실패하는 특수 사례인지, 권한 경계인지, 별도 창 모델인지 구분할 수 있습니다.
