# FlowLens 행동 분석 확장 설계

## 이번 구현에 포함된 지표

FlowLens는 Windows에서 활성 창을 1초 간격으로 확인하고, 앱 전환 이벤트와 함께 분당 스냅샷을 로컬 `activity.json`에 저장합니다. 이번 확장에는 앱별 활성 시간, 앱 전환 횟수, 분당 활성 앱 수, 마우스 이동 거리(px), 마우스 클릭 횟수와 평균 클릭 간격, 키보드 이벤트 수, 네트워크 인터페이스 전체 수신·송신 바이트, 시간대별 포커스 시간 및 타임라인이 포함됩니다.

네트워크 총량은 Windows IP Helper 인터페이스 카운터의 누적 수신·송신 바이트를 읽어 계산합니다. 이 값은 모든 네트워크 인터페이스의 합계이며, 현재 버전은 프로세스별 네트워크 바이트를 임의로 추정하지 않습니다.

## 아직 별도 권한이 필요한 수집 항목

Windows 알림 횟수와 프로세스별 네트워크 사용량은 일반적인 활성 창 API만으로는 신뢰성 있게 얻을 수 없습니다. 알림 횟수는 Windows User Notification Listener 권한과 앱 매니페스트 capability가 필요하고, 앱별 네트워크 사용량은 ETW 또는 WFP 기반 수집기를 사용해야 합니다. 이 두 기능은 사용자에게 권한을 명시적으로 안내하고, 수집 범위와 민감정보를 검토한 뒤 별도 모듈로 추가하는 것이 안전합니다. 현재 UI는 알림 카운터를 준비 상태로 표시하며, 0을 실제 수집값처럼 가장하지 않습니다.

## 추가로 권장하는 행동 지표

| 지표 | 의미 | 개인정보 보호 방식 |
|---|---|---|
| 유휴 시간 | 입력·창 전환이 없는 구간 | 임계값과 지속시간만 저장 |
| 컨텍스트 스위치 | 짧은 시간 내 앱 반복 전환 | 앱 식별자와 횟수만 저장 |
| 작업 세션 | 연속 활동 구간의 시작·종료·길이 | 창 제목은 선택적 마스킹 |
| 재개 시간 | 중단 후 다시 작업하기까지의 시간 | 이벤트 간격만 저장 |
| 키보드 리듬 | 분당 이벤트 수와 변동성 | 키 내용·키 코드는 저장하지 않음 |
| 클릭 리듬 | 클릭 간격, burst, 이동 후 클릭까지 시간 | 좌표 대신 거리·간격만 저장 |
| 창 크기/모니터 | 작업 환경과 멀티태스킹 패턴 | 창 위치는 격자화하거나 저장하지 않음 |
| 회의/통화 상태 | 특정 앱의 연속 활성 세션 | 콘텐츠·참석자·음성은 수집하지 않음 |
| 빌드/테스트 신호 | 개발 도구의 특정 작업 반복 | 명령어·파일명 대신 이벤트 카테고리만 저장 |
| 수면/근무 경계 | 시간대별 활동 시작·종료 | 정확한 제목 대신 시간 버킷 사용 |

## 임베딩 분석 단계

임베딩은 원천 이벤트를 그대로 외부 모델에 보내는 방식이 아니라, 먼저 로컬에서 하루 단위 특징 벡터를 만들고 사용자가 명시적으로 분석을 활성화했을 때만 수행해야 합니다. 권장 특징은 앱별 시간 비율, 전환 엔트로피, 세션 길이의 평균·분산, 유휴 비율, 키보드·마우스 밀도, 클릭 간격 분산, 네트워크 총량의 시간대별 변화, 알림/중단 횟수, 시간대별 집중 점수입니다.

저장 구조는 `raw events → minute snapshots → daily feature vector → embedding`의 4계층으로 나누는 것이 좋습니다. 원천 이벤트와 임베딩 모두 로컬에 저장하고, 임베딩 생성기에는 원본 창 제목을 전달하지 않으며 앱 카테고리와 시간 버킷으로 축약합니다. 임베딩 모델도 기본값은 로컬 실행으로 두고, 외부 API 사용은 별도의 명시적 동의와 전송 범위 미리보기를 거쳐야 합니다.

분석 결과는 생산성 점수처럼 단정적인 평가보다 “오후 2~4시에 컨텍스트 스위치가 증가한다”, “긴 세션 전에 마우스 이동과 클릭 burst가 증가한다”와 같은 설명 가능한 특징점과 근거 이벤트 개수로 표시하는 것이 적절합니다. 원본 데이터 삭제 시 특징 벡터와 임베딩도 함께 삭제되어야 합니다.
