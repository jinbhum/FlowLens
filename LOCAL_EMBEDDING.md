# FlowLens 로컬 업무 패턴 임베딩

## 목적과 경계

FlowLens의 업무 패턴 분석은 사용자의 원천 활동을 외부 인공지능 서비스로 보내지 않습니다. 앱은 이미 로컬에서 계산한 일일 특징을 **25차원 수치형 임베딩**으로 정규화하고, 코사인 유사도로 현재 업무일을 과거 업무일 및 개인 기준선과 비교합니다.

> 이 기능은 생산성 점수나 감시 평가를 생성하지 않습니다. 반복되는 업무 리듬, 집중 시간 변화, 유휴 비율, 컨텍스트 전환, 세션 구조처럼 사용자가 확인할 수 있는 패턴과 그 수치적 근거를 표시합니다.

## 분석 활성화와 저장

임베딩 분석은 기본값이 **꺼짐**입니다. 사용자는 앱의 패턴 분석 영역 또는 설정에서 수집 범위가 표시된 확인 창을 보고 직접 활성화해야 합니다. 활성화 후에는 아래 항목이 Windows 앱 데이터 폴더의 기존 `activity.json` 안에만 저장됩니다.

- 일자와 생성 시각
- 25차원 정규화 벡터
- 입력 특징의 수치형 사본
- 데이터 신뢰도(활성 집중 시간 기반)

하루별 레코드는 최대 365개까지 보관합니다. 분석 데이터만 삭제하면 일별 벡터, 기준선, 유사 업무일 이력만 삭제되며 원천 활동 데이터는 유지됩니다. 전체 삭제는 원천 이벤트와 임베딩 이력을 함께 삭제합니다.

## 25차원 벡터 구성

| 범주 | 차원 | 정규화 방식 |
|---|---:|---|
| 집중·유휴 | 3 | 집중 시간 로그 스케일, 유휴 시간, 활성 비율 |
| 앱·전환 | 4 | 앱 수, 활성 시간당 전환 수, 평균 세션 길이, 세션 수 |
| 입력 리듬 | 5 | 키보드 이벤트 밀도, 마우스 거리 밀도, 클릭 밀도, 클릭 간격 분산, 휠 노치 밀도 |
| 네트워크 | 1 | 활성 분당 전체 인터페이스 바이트 로그 스케일 |
| 시간대 | 4 | 피크 집중 시간의 sin/cos, 시작 시간, 작업 시간 폭 |
| 앱 시간 분포 | 8 | 가장 큰 앱 시간 비율 8개 |

앱 시간 분포는 “어떤 앱”의 이름이 아니라 내림차순 **사용 시간 비율**만 사용합니다. 피크 시간은 24시간의 순환성을 고려해 sin/cos 두 값으로 표현합니다. 모든 연속값은 사전 정의한 상한으로 0~1 범위에 정규화됩니다.

## 비교와 인사이트

현재 임베딩과 과거 임베딩 사이의 코사인 유사도를 구해 상위 3개의 유사 업무일을 표시합니다. 최소 두 번째 업무일부터는 과거 벡터 평균으로 개인 기준선을 만들어 다음을 설명 가능한 문장으로 표시합니다.

- 집중 시간이 기준선보다 20% 이상 길거나 25% 이상 짧은 경우
- 활성 1시간당 컨텍스트 전환이 기준선보다 증가한 경우
- 활성 비율이 기준선보다 12%p 이상 하락한 경우
- 유사도 90% 이상인 반복 업무 리듬
- 단일 도구 집중, 짧게 분할된 세션

활성 집중 시간이 15분 이상이면 현재 업무일의 집중 시간·활성 비율, 세션 구조·전환 밀도, 개인 기준선 대비 차이를 근거로 **2~3줄의 로컬 업무 평가**를 함께 표시합니다. 이 평가는 규칙 기반의 수치 요약이며 건강, 피로, 성과나 생산성을 진단·판정하지 않습니다. 분석 결과는 데이터가 충분하지 않으면 “기준선 학습 중”으로 표시합니다. 초기 데이터가 없을 때 추정값이나 데모 패턴을 표시하지 않습니다.

## 포함하지 않는 정보

임베딩 계산기와 분석 결과에는 다음 데이터가 전달되지 않거나 저장되지 않습니다.

- 창 제목, 앱의 텍스트 콘텐츠, 문서 이름
- 키 입력 문자열 및 키 코드
- 브라우저 URL, 검색어, IP 주소, 패킷 내용
- 화면 캡처, 음성, 알림 본문
- 마우스 절대 좌표
- 휠 방향, 스크롤 대상·내용, 스크롤 좌표

이 기능은 네트워크 라이브러리, 분석 SDK, 원격 데이터베이스, 외부 LLM API를 사용하지 않습니다. 향후 외부 AI 기반 분석을 제공한다면, 전송 대상·특징값 미리보기·보존 기간·명시적 동의를 별도의 선택 기능으로 제공해야 합니다.
