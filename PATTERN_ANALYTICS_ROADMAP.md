# FlowLens 업무 패턴 분석 확장 로드맵

**작성일:** 2026-09-17  
**작성자:** Manus AI

## 결론

FlowLens의 다음 단계는 더 많은 원천 데이터를 모으는 것이 아니라, 이미 수집하는 활동을 **작업 흐름의 구조**로 바꾸는 일이어야 합니다. 가장 가치가 높은 신호는 특정 앱을 얼마나 오래 열었는지가 아니라, 사용자가 **어떤 방식으로 일을 시작하고, 중단을 회복하고, 도구를 오가며, 하루를 마무리하는지**를 보여 주는 신호입니다.

권장 방향은 세 가지입니다. 첫째, 원본 창 제목이나 입력 내용을 보지 않고도 계산할 수 있는 **전환 순서·세션 구조·시간대 변화**를 추가합니다. 둘째, 사용자가 선택한 소수의 맥락 태그를 결합해 “무슨 종류의 업무일이었는가”를 비교합니다. 셋째, 개인 평균 하나와 비교하지 않고 요일·업무 모드·시간대별 기준선을 분리해 변화점과 반복 패턴을 설명합니다.

> FlowLens는 사용자를 평가하는 생산성 점수를 만들어서는 안 됩니다. 분석 결과는 “오후 회의 뒤 재진입에 평소보다 11분이 더 걸렸다”처럼, 사용자가 검증하고 조정할 수 있는 근거 중심 문장이어야 합니다.

## 설계 원칙

모든 신규 항목은 **로컬 우선**, **명시적 활성화**, **최소 수집**, **개별 삭제**, **분석 근거 표시**라는 다섯 원칙을 따라야 합니다. 현재 Windows 세션의 유휴 상태는 `GetLastInputInfo`로 확인할 수 있지만, 이 값은 호출한 세션 안의 입력만 나타내며 값이 항상 단조 증가한다고 보장되지 않습니다. 따라서 FlowLens는 이를 절대적인 근무 사실이 아니라 세션 내 활동 신호로만 해석해야 합니다.[1]

알림이나 시스템 전반 이벤트처럼 권한 범위가 넓은 데이터는 기본 기능으로 섞으면 안 됩니다. Windows 알림 리스너는 다른 앱의 알림에 접근할 수 있으므로 앱 매니페스트 capability와 사용자 동의가 필요합니다.[2] ETW는 프로세스 생성·종료, 메모리, 컨텍스트 전환 등 시스템 이벤트를 구조적으로 제공하지만 데이터량과 민감도가 높습니다.[3] 따라서 고급 수집기는 별도 설치·권한·보존 기간·삭제 경로를 갖는 선택 기능으로 분리해야 합니다.

## 가장 먼저 추가할 만한 신호

### 1. 작업 재진입 비용

사용자가 유휴 상태, 회의, 앱 전환 뒤 다시 집중 상태로 들어가기까지 걸리는 시간을 기록합니다. 단순한 유휴 시간보다 중요한 이유는 같은 20분 휴식도 이후 1분 만에 작업으로 복귀하는 경우와 18분 동안 앱을 반복 전환하는 경우가 전혀 다르기 때문입니다.

**수집값**은 세션 종료 시각, 다음 지속 활동 시작 시각, 복귀 뒤 첫 10분의 전환 수와 입력 밀도입니다. 원본 창 제목이나 키 입력은 필요하지 않습니다. 이 신호로 “회의 뒤 평균 재진입 14분”, “짧은 휴식 뒤 재진입 4분”, “재진입 직후 전환 과다” 같은 통계를 만들 수 있습니다.

### 2. 업무 흐름 전이 그래프

앱 이름 대신 사용자가 로컬에서 지정한 앱 카테고리만 사용합니다. 예를 들어 `개발`, `터미널`, `문서`, `메신저`, `브라우저`, `회의`, `디자인`, `운영`처럼 분류합니다. 이후 `개발 → 터미널 → 브라우저 → 개발` 같은 연속 전이를 집계해 개인의 반복 작업 루프를 만듭니다.

이 방식은 “IDE를 많이 사용했다”보다 더 유용합니다. 예를 들어 특정 날에 `개발 → 메신저 → 개발` 전이가 급증했다면 협업 방해가 늘어난 상황일 수 있고, `개발 → 터미널 → 개발` 루프가 안정적으로 길어진다면 구현·검증 작업이 진행된 상황일 수 있습니다. 저장 대상은 카테고리 간 전이 횟수와 전이 간격뿐이며, 창 제목·URL·대화 내용은 저장하지 않습니다.

### 3. 컨텍스트 전환의 질

현재 FlowLens는 전환 횟수를 수집합니다. 여기에 **전환 간격 분포**, **짧은 왕복 전환 비율**, **전환 엔트로피**, **복귀율**을 더하면 전환의 성격을 구분할 수 있습니다.

짧은 왕복 전환은 2분 안에 같은 두 카테고리를 반복하는 패턴입니다. 전환 엔트로피는 전환 대상이 몇 개 카테고리에 얼마나 고르게 퍼져 있는지를 나타냅니다. 복귀율은 특정 도구를 떠난 뒤 일정 시간 안에 다시 돌아오는 비율입니다. 이 조합은 “도구가 다양했다”와 “작업이 분산되어 재진입을 반복했다”를 분리해 줍니다.

### 4. 집중 세션의 형태

평균 세션 길이 하나로는 충분하지 않습니다. 세션 길이의 중앙값, 상위 10% 길이, 변동계수, 세션 사이 휴식 길이, 첫 세션까지 걸린 시간을 함께 계산하면 하루의 작업 구조를 볼 수 있습니다.

추천 통계는 **롱폼 집중 비율**입니다. 이는 25분, 45분, 90분 이상인 세션이 전체 집중 시간에서 차지하는 비율입니다. 또 하나는 **회복형 휴식 비율**입니다. 이는 3~15분 휴식 뒤 다음 20분에 유지된 활동 비율을 비교하는 통계입니다. 이 값은 휴식이 “좋다”는 판단이 아니라, 해당 사용자에게 어떤 길이의 중단 뒤 재진입이 부드러운지 찾기 위한 개인화 신호입니다.

### 5. 시간대별 작업 모드

하루를 하나의 벡터로만 저장하는 대신, 오전·정오·오후·야간과 같이 4~6개 시간 버킷별 미니 벡터를 만듭니다. 각 버킷은 집중 시간, 전환 밀도, 앱 카테고리 분포, 입력 리듬, 네트워크 밀도만 포함합니다.

이 구조는 “오후 2~4시에 집중 시간이 떨어진다”보다 구체적인 관찰을 가능하게 합니다. 예를 들어 “오후에는 문서·메신저 비율이 증가하고 전환 엔트로피가 높아진다”, “오전의 개발 루프는 세션이 길고 복귀율이 낮다”처럼 시간대에 따른 업무 모드를 비교할 수 있습니다.

### 6. 입력 리듬의 안정성

키보드·마우스 총량은 작업량이 아닙니다. 대신 5분 단위로 입력 밀도의 중앙값, 변동계수, 연속 무입력 구간 수를 계산합니다. 이를 사용하면 장시간의 고밀도 입력, 짧은 burst가 반복되는 작업, 읽기·검토 중심의 저입력 집중을 구분할 수 있습니다.

분석 결과는 “입력이 적어서 비생산적”이라고 표현하면 안 됩니다. 적절한 표현은 “이 시간대는 저입력·저전환의 장기 세션으로, 읽기·검토형 작업 리듬과 유사하다”입니다. 해석의 확실성이 낮을 때는 추정 레이블 대신 수치만 보여야 합니다.

### 7. 마우스 경로의 목적성 지표

절대 좌표를 저장하지 않고도 마우스 이동 거리, 클릭 수, 클릭 간 평균 거리, 이동-클릭 비율을 이용할 수 있습니다. 예를 들어 이동 거리가 매우 길고 클릭이 적으면 탐색·스크롤 중심의 상호작용일 수 있습니다. 반대로 짧은 이동과 빠른 클릭이 반복되면 조작·편집형 리듬일 수 있습니다.

이 신호는 “피로”를 진단하는 데 사용하면 안 됩니다. 대신 앱·시간대별 상호작용 방식의 변화나, 이전 유사 업무일과의 차이를 설명하는 보조 특징으로 사용해야 합니다.

### 8. 작업 환경 안정성

추가 동의 후 모니터 수, 해상도 버킷, 도킹 여부, 전원 연결 상태, 배터리 구간, 화면 잠금·해제 횟수를 집계할 수 있습니다. 장치 이름, Wi-Fi SSID, 블루투스 기기 이름, 정확한 창 좌표는 저장하지 않습니다.

이 데이터로 “외부 모니터가 연결된 날에는 긴 개발 세션 비율이 높았다”, “배터리 상태에서 전환 밀도가 증가했다” 같은 개인 관찰을 만들 수 있습니다. 다만 이는 상관관계이며 원인으로 단정해서는 안 됩니다.

### 9. 시스템 부담과 작업 흐름의 관계

고급 선택 기능으로 활성 앱의 CPU·메모리·디스크 I/O를 1분 집계로 수집할 수 있습니다. 저장 대상은 프로세스별 원시 명령줄이 아니라 사용자가 승인한 앱 카테고리별 부하 버킷이어야 합니다. 예를 들어 CPU 사용률은 `낮음`, `보통`, `높음`으로 양자화할 수 있습니다.

이 신호는 “시스템 대기 시간”을 찾는 데 유용합니다. 고부하 구간 직후 앱 전환과 유휴가 함께 증가한다면 작업자 행동이 아니라 빌드·동기화·렌더링 대기일 수 있습니다. ETW는 이런 시스템 수준 상호작용 분석에 적합하지만, 원시 이벤트는 저장하지 않고 분당 집계만 남겨야 합니다.[3]

### 10. 중단 비용 지도

알림 횟수, 회의 카테고리 활성, 잠금·해제, 짧은 앱 왕복, 네트워크 burst를 시간 축에 겹쳐 **중단 지도**를 만듭니다. 결과는 “오후 3시대에 중단이 많다”가 아니라 “오후 3시대에 3분 미만 중단 7회 뒤 재진입 시간이 기준선보다 8분 길었다”처럼 표시합니다.

알림 데이터는 반드시 MSIX 기반 동의 이후에도 **앱별 개수와 추가·해제 시각 버킷**만 사용해야 합니다. 알림 제목·본문·이미지는 수집하거나 표시하지 않습니다.[2]

### 11. 사용자가 직접 남기는 한 번의 맥락 태그

자동 수집만으로는 업무의 의도를 알기 어렵습니다. 하루 시작 또는 세션 종료 때 사용자가 한 번만 누르는 태그를 제공하면 분석 품질이 크게 올라갑니다. 추천 태그는 `구현`, `디버깅`, `문서화`, `검토`, `회의`, `학습`, `운영 대응` 정도입니다.

이 태그는 완전히 선택 사항이어야 하며, 자유 텍스트보다 미리 정의된 범주가 안전합니다. 사용자가 태그를 남긴 날에는 같은 업무 모드끼리만 기준선을 비교할 수 있습니다. 예를 들어 회의 중심 날을 개발 집중일과 비교하는 왜곡을 줄일 수 있습니다.

### 12. 일일 자가 평가 2문항

사용자가 원한다면 업무 종료 시 `오늘 목표의 명확성`, `오늘 작업 흐름의 만족도`를 1~5점으로 기록하게 할 수 있습니다. 건강·정신 상태·의료적 판단을 묻지 않고, 점수는 로컬 모델의 결과 레이블로만 사용합니다.

이 데이터는 임베딩의 유용성을 검증하는 데 특히 좋습니다. 예를 들어 “전환 횟수보다 재진입 비용이 만족도와 더 강하게 동행한다” 같은 개인 수준의 통찰을 만들 수 있습니다. 다만 표본이 적으면 상관계수나 예측 결과를 제시하지 말고, 최소 20~30개의 태그가 쌓인 뒤에만 표시해야 합니다.

## 권장 통계와 분석 기법

| 분석 방향 | 계산 단위 | 사용자에게 보여 줄 수 있는 결과 | 해석 시 주의점 |
|---|---|---|---|
| 업무 전이 시퀀스 | 앱 카테고리 전이 | 반복되는 도구 루프, 평소와 다른 흐름 | 앱 이름·창 제목을 시퀀스에 넣지 않음 |
| 재진입 비용 | 중단 후 다음 집중 구간 | 중단 유형별 복귀 시간 | 인과관계가 아니라 연관성으로 표현 |
| 집중 세션 분포 | 세션 | 긴 세션 비율, 세션 길이 변동 | 입력량을 작업 성과로 해석하지 않음 |
| 시간대별 미니 임베딩 | 2~4시간 버킷 | 오전/오후/야간 업무 모드 | 야간 활동을 부정적으로 평가하지 않음 |
| 변화점 탐지 | 일·주 단위 | 평소 리듬에서 벗어난 시작 시점 | 최소 14일 이상의 기준 데이터 필요 |
| 유사 업무일 검색 | 일일 벡터 | 가장 닮은 과거 업무일 3개 | 유사도는 품질이나 성과 점수가 아님 |
| 회복형 휴식 분석 | 휴식 뒤 20~30분 | 개인에게 맞는 휴식 길이 후보 | 일정·회의 등 외부 요인을 함께 고려 |
| 시스템 대기 분석 | 분당 성능 버킷 | 작업 흐름을 끊는 고부하 구간 | ETW 사용 시 별도 동의·보존 제한 필요 |
| 자가 태그 기반 비교 | 업무 모드별 일 단위 | 구현일·검토일별 개인 기준선 | 태그가 없는 날은 무리하게 분류하지 않음 |
| 변화 설명 카드 | 분석 결과 | “전환 증가”, “재진입 지연”의 수치 근거 | 단정적 조언보다 관찰 문장 사용 |

## 임베딩 확장 방향

현재 25차원 일일 벡터는 좋은 시작점이지만, 다음 단계에서는 하나의 큰 벡터만 늘리기보다 **계층형 임베딩**을 권장합니다.

첫 계층은 현재처럼 하루의 전체 리듬을 나타내는 일일 벡터입니다. 둘째 계층은 오전·오후 등 시간 버킷의 미니 벡터입니다. 셋째 계층은 앱 카테고리 전이를 집계한 시퀀스 벡터입니다. 넷째 계층은 사용자가 선택한 업무 모드 태그입니다. 검색과 비교는 같은 업무 모드와 요일을 우선하고, 그 안에서 일일 벡터와 시퀀스 벡터의 가중 유사도를 계산합니다.

가중치는 고정값보다 사용자가 조정할 수 있어야 합니다. 예를 들어 개발자는 앱 전이와 긴 세션을 더 중요하게 보고, 기획·문서 중심 사용자는 시간대·읽기형 저입력 세션을 더 중요하게 볼 수 있습니다. 단, 초기 버전은 자동 학습보다 설명 가능한 고정 가중치로 시작하는 편이 안전합니다.

## 구현 우선순위

| 단계 | 구현 항목 | 권한 | 기대 가치 | 권장 보관 |
|---|---|---|---|---|
| P0 | 앱 카테고리, 전이 그래프, 재진입 비용, 세션 분포, 시간대별 미니 벡터 | 기본 추적 범위 | 매우 높음 | 분당 집계 90일, 일일 벡터 365일 |
| P1 | 사용자가 누르는 업무 모드 태그, 2문항 자가 평가, 변화점 탐지 | 명시적 선택 | 높음 | 태그·평가 365일 |
| P2 | 화면 잠금·전원·도킹 상태, 시스템 성능 버킷 | 별도 선택 | 중간 | 분당 집계 30~90일 |
| P3 | 알림 수, 앱별 네트워크, ETW 기반 시스템 대기 분석 | MSIX 동의 또는 UAC 관리자 승인 | 상황 의존 | 분당 집계 7~30일, 원시 이벤트 저장 금지 |

P0는 현재의 로컬 추적 및 임베딩 구조만 확장하면 되므로 가장 먼저 구현할 가치가 있습니다. 특히 앱 카테고리는 사용자가 직접 편집하는 로컬 매핑 테이블로 만들고, 기본값은 `미분류`로 두는 방식이 좋습니다. 자동 분류를 추가하더라도 앱 실행 파일의 메타데이터를 외부에 보내지 않아야 합니다.

## 수집하지 말아야 할 항목

다음 정보는 패턴 분석의 효용보다 침해 위험이 크므로 기본 설계에서 제외해야 합니다.

| 제외 항목 | 제외 이유 | 대체 신호 |
|---|---|---|
| 키 입력 내용·클립보드 | 비밀번호, 개인 정보, 업무 기밀 노출 위험 | 입력 이벤트 수·시간 간격 |
| 화면 캡처·OCR | 문서·대화·고객 정보 노출 위험 | 앱 카테고리·세션 길이 |
| 브라우저 URL·검색어 | 개인·업무 검색 이력 노출 위험 | 브라우저 카테고리 활성 시간 |
| 알림 제목·본문 | 제3자 정보와 민감 대화 노출 위험 | 앱별 알림 개수·시간 버킷 |
| 패킷·IP·도메인 | 네트워크 활동의 과도한 감시 | 앱 카테고리별 바이트 집계 |
| 마우스 절대 좌표 | 화면 내용과 작업 대상 추론 가능 | 이동 거리·클릭 간격 |
| 마이크·카메라 데이터 | 침해 위험이 매우 큼 | 회의 앱 카테고리의 활성 시간 |

## 제품 화면 제안

패턴 분석 화면은 하나의 큰 점수보다 다음 네 구역으로 구성하는 것이 좋습니다. 첫 구역은 오늘의 업무 리듬을 “집중형”, “전환형”, “협업형” 같은 **중립적 구조**로 보여 주되, 데이터가 충분하지 않으면 라벨 대신 수치만 보여 줍니다. 둘째 구역은 유사 업무일과의 차이를 집중 시간, 전환 밀도, 재진입 비용으로 나란히 보여 줍니다. 셋째 구역은 시간대별 흐름과 전이 그래프를 표시합니다. 넷째 구역은 “어떤 데이터가 이 설명을 만들었는가”를 제공하고, 해당 특징·태그·분석 이력을 바로 삭제할 수 있게 합니다.

사용자 알림은 일일 요약보다 변화점 중심이 적절합니다. 예를 들어 “최근 5개 구현일과 비교하면 오후 재진입 시간이 길어졌습니다”처럼 구체적 기준을 함께 보여 주어야 합니다. 알림 빈도는 기본 주 1회 이하로 제한하고, 사용자가 끌 수 있어야 합니다.

## 검증 기준

새 기능은 다음 기준을 통과해야 제품에 넣을 수 있습니다. 첫째, 어느 인사이트든 사용자가 원인 데이터를 수치로 확인할 수 있어야 합니다. 둘째, 원천 콘텐츠 없이도 계산되어야 합니다. 셋째, 최소 표본 미달 시 결론을 내리지 않아야 합니다. 넷째, 특정 요일·업무 태그·시간대의 차이를 개인의 능력이나 건강 상태로 해석하지 않아야 합니다. 다섯째, 분석 이력만 지우는 기능과 전체 데이터 삭제 기능이 분리되어야 합니다.

## 권장 다음 구현 단위

가장 효율적인 다음 릴리스는 **P0 작업 흐름 분석**입니다. 구체적으로는 로컬 앱 카테고리 매핑, 카테고리 전이 행렬, 중단 뒤 재진입 비용, 시간대별 미니 임베딩, 전환 엔트로피, 긴 세션 비율을 추가합니다. 이 조합은 새로운 관리자 권한이나 외부 서비스 없이 현재의 `activity.json → daily feature → local embedding` 구조 위에서 구현할 수 있습니다.

## References

[1]: https://learn.microsoft.com/en-us/windows/win32/api/winuser/nf-winuser-getlastinputinfo "GetLastInputInfo function (winuser.h)"
[2]: https://learn.microsoft.com/en-us/windows/apps/develop/notifications/app-notifications/notification-listener "Notification listener: Access all notifications"
[3]: https://learn.microsoft.com/en-us/windows/apps/trace-processing/overview "Process ETW traces in .NET"
