# FlowLens 보안검토 결과

검토 대상은 FlowLens v0.3.2 소스, Tauri 설정, Windows 릴리스 워크플로 및 런타임 의존성 선언입니다. 검토일은 2026-09-17입니다.

## 결론

현재 앱 코드에는 수집된 활동 정보를 설치된 기기 외부로 전송하는 구현이 확인되지 않았습니다. 추적 데이터는 Windows 앱 데이터 폴더의 `activity.json`에 원자적 교체 방식으로 저장되며, 서버 API, 원격 데이터베이스, HTTP 클라이언트, WebSocket, 분석 SDK, 오류 보고 SDK, 자동 업데이트 플러그인이 포함되어 있지 않습니다.

프론트엔드의 `invoke` 호출은 Tauri IPC를 통해 로컬 Rust 명령을 호출하는 것이며 외부 네트워크 요청이 아닙니다. `get_tracking_state`, `set_tracking`, `clear_all_data`는 모두 설치된 앱 내부의 로컬 명령입니다.

## 점검 항목

| 영역 | 결과 | 설명 |
|---|---|---|
| 외부 HTTP/API 전송 | 없음 | `fetch`, Axios, reqwest, WebSocket, `sendBeacon` 등 사용 없음 |
| 텔레메트리/분석 | 없음 | Sentry, PostHog, Segment 등 SDK 없음 |
| 자동 업데이트 | 없음 | Tauri updater 플러그인 및 업데이트 엔드포인트 없음 |
| 로컬 데이터 저장 | 로컬 전용 | Windows 앱 데이터 폴더의 `activity.json`에만 저장 |
| 추적 데이터 범위 | 집계형 | 활성 창 제목, 앱 전환, 분당 열린 창 수, 포커싱 시간, 키보드 이벤트 수, 마우스 이동 거리·클릭 간격, 네트워크 인터페이스 송수신 총량 |
| 키보드 내용 | 수집하지 않음 | 키 코드와 입력 문자열을 저장하지 않고 이벤트 발생 여부만 집계 |
| 마우스 위치 | 저장하지 않음 | 절대 좌표는 저장하지 않고 픽셀 이동 거리와 클릭 간격만 집계 |
| 네트워크 내용 | 수집하지 않음 | URL, IP, 패킷 내용 없이 Windows 인터페이스 바이트 카운터의 차이만 저장 |
| 화면 캡처 | 수집하지 않음 | screenshot, desktop duplication, OCR 구현 없음 |
| 브라우저 URL | 수집하지 않음 | 현재 구현은 브라우저의 실제 URL을 읽지 않음 |
| 콘텐츠 보안 정책 | 제한 | 로컬 리소스와 Tauri IPC만 허용하도록 CSP 설정 |
| 전체 삭제 | 제공 | 설정 페이지의 전체 삭제가 집계 상태를 초기화하고 `activity.json`을 삭제 |

## 발견 및 조치

기존 CSS에는 Google Fonts CDN을 향한 `@import`가 있었으므로, 앱이 실행될 때 외부 폰트 요청이 발생할 가능성이 있었습니다. 이를 제거하고 시스템 폰트 fallback으로 변경했습니다. 따라서 배포 빌드의 화면 렌더링을 위해 외부 폰트 네트워크 접근이 필요하지 않습니다.

## 잔여 고려사항

GitHub Actions 워크플로는 빌드 과정에서 GitHub Actions와 npm/Rust 패키지 저장소에 접근하고, 설치파일을 GitHub Actions 아티팩트 또는 GitHub Release에 업로드합니다. 이는 개발/배포 인프라의 통신이며 설치된 FlowLens 앱의 사용자 활동 데이터 전송 경로는 아닙니다.

현재 `active_window`와 앱별 `title`에 창 제목 전체가 저장될 수 있으므로 창 제목에 민감정보가 포함되는 환경에서는 개인정보 노출 가능성이 남아 있습니다. 정식 배포 전에는 앱 이름만 허용하는 정규화, 민감 창 제외 목록, 추적 권한 안내 및 로컬 데이터 보존 기간 정책을 추가하는 것이 권장됩니다.

또한 Windows 빌드 산출물에 대해 코드 서명 인증서가 적용되지 않았습니다. 배포 전에는 조직의 인증서로 서명하고 해시를 함께 제공해야 공급망 변조와 SmartScreen 경고 위험을 줄일 수 있습니다.
