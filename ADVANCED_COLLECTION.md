# FlowLens 고급 수집 및 로컬 특징 벡터

## 기본 원칙

FlowLens의 기본 추적기는 **관리자 권한 없이** 실행됩니다. 활성 창, 앱 전환, 입력량, 유휴 구간, 마우스 이동 거리, 클릭 간격, 분당 열린 앱 창 수 및 네트워크 인터페이스 전체 바이트를 수집하지만, 화면 내용, 문서 내용, 키 입력 문자열, 키 코드, 마우스 절대 좌표, URL, IP 주소, 패킷 내용은 저장하지 않습니다.

원천 이벤트와 분당 스냅샷, 작업 세션, 일일 특징 벡터는 Windows 앱 데이터 폴더의 `activity.json`에만 기록됩니다. 설정의 **전체 삭제**는 원천 이벤트·세션·특징 벡터·고급 수집 권한 상태를 함께 삭제합니다.

## 알림 횟수: 관리자 권한이 아닌 Windows 동의

Windows 알림에 접근하는 `UserNotificationListener`는 다른 앱의 알림에 접근할 수 있으므로, 사용자 동의가 필수입니다. Microsoft의 공식 요구사항은 다음과 같습니다.

1. MSIX 패키지의 매니페스트에 **User Notification Listener** capability를 선언합니다.
2. 첫 실행 시 UI 스레드에서 Windows의 알림 접근 동의를 요청합니다.
3. 사용자가 거부했거나 나중에 Windows 설정에서 해제하면 기능을 비활성으로 유지합니다.
4. 알림 텍스트·본문·첨부파일을 저장하지 않고, 앱 식별자와 생성/해제 이벤트 수만 집계합니다.

따라서 현재 MSI/NSIS EXE 설치본에서는 알림 수집을 자동으로 시도하지 않습니다. 이 기능은 별도 **MSIX 배포 채널**과 명시적 동의 화면을 준비한 후에만 활성화해야 합니다. 공식 문서: [Notification listener: Access all notifications](https://learn.microsoft.com/en-us/windows/apps/develop/notifications/app-notifications/notification-listener).

## 앱별 네트워크 사용량: 선택적 관리자 권한 ETW 수집기

프로세스별 송·수신 바이트는 일반적인 Windows 인터페이스 카운터만으로 정확히 측정할 수 없습니다. FlowLens는 이 기능을 **ETW(Event Tracing for Windows)** 기반의 별도 수집기로 제한합니다.

권장 배포 순서는 다음과 같습니다.

1. 사용자가 설정 화면에서 “앱별 네트워크 사용량”을 켭니다.
2. 앱은 수집 범위, 보관 기간, 저장 위치, 제외 데이터(URL/IP/패킷)를 다시 표시합니다.
3. 사용자가 동의하면 별도 서명된 `FlowLensNetworkCollector.exe`를 UAC elevation으로 시작합니다.
4. 수집기는 ETW의 네트워크 이벤트에서 PID·바이트 수·시간만 집계하고, PID를 현재 프로세스 이름으로 로컬 매핑합니다.
5. 수집기는 집계된 분당 `process_network.json`만 `activity.json`과 같은 앱 데이터 폴더에 쓰고, 원시 ETW 이벤트는 디스크에 남기지 않습니다.
6. 사용자가 중지하거나 전체 삭제를 실행하면 수집기 종료 및 집계 파일 삭제를 수행합니다.

ETW는 Windows에 내장된 구조화 시스템 이벤트 수집 체계로, 시스템 차원의 성능·네트워크 분석에 쓰입니다. 공식 개요: [Process ETW traces in .NET](https://learn.microsoft.com/en-us/windows/apps/trace-processing/overview).

> 현재 v0.4.0은 사용자의 동의 상태와 관리자 권한 필요성을 UI에 명시하고, 권한 없이 이 수집기를 자동 시작하지 않습니다. 실제 ETW 헬퍼는 코드 서명·UAC manifest·프로세스 수명 관리·ETW 공급자별 검증이 필요한 후속 배포 단위입니다.

## 구현된 주요 특징점

| 특징점 | 구현 상태 | 계산 방식 |
|---|---|---|
| 유휴 시간 | 구현 | 마지막 키보드/마우스 입력 뒤 5분 이상인 구간을 1초 단위로 집계 |
| 작업 세션 | 구현 | 유휴 상태가 아닌 연속 작업 구간의 시작·종료·활성 시간·앱 다양성·전환 횟수 |
| 컨텍스트 스위치 | 구현 | 활성 창이 변경된 횟수 및 활성 1시간당 전환 밀도 |
| 재개 지연 | 구현 | 마지막 입력 이후 새 작업 세션이 시작될 때까지의 시간 |
| 키보드 리듬 | 구현 | 키 문자열 없이 눌림 시작 이벤트 수/활성 분 |
| 클릭 리듬 | 구현 | 클릭 상승 에지의 평균 간격 및 Welford 온라인 분산 |
| 마우스 행동 | 구현 | 절대 좌표 없이 픽셀 이동 거리/활성 분 |
| 앱 시간 분포 | 구현 | 앱별 활성 시간 비율 상위 12개 |
| 시간대 집중 | 구현 | 시간별 활성 초, 피크 집중 시간, 시작/종료 시간대 |
| 네트워크 밀도 | 구현 | 인터페이스 전체 송·수신 바이트/활성 분 |
| 알림 수 | 권한 설계 완료 | MSIX capability와 Windows 동의 후 앱별 이벤트 수만 집계 |
| 앱별 네트워크 | 권한 설계 완료 | UAC 승인 ETW 헬퍼로 PID별 바이트만 집계 |

## 임베딩 전 처리

일일 특징 벡터는 `DailyFeatureVector`로 저장됩니다. 이 벡터는 다음의 수치형 특징만 포함합니다.

- 집중·유휴 시간과 활성 비율
- 앱 수, 앱 시간 점유율, 컨텍스트 스위치 밀도
- 세션 평균 길이와 재개 지연
- 키보드 이벤트 밀도, 마우스 이동 밀도, 클릭 간격 분산
- 네트워크 바이트 밀도
- 피크 집중 시간, 업무 시작·종료 시간대

벡터 생성기에는 창 제목이나 키 입력 내용이 전달되지 않습니다. 향후 임베딩 모델을 연결할 때에도 기본 정책은 **로컬 실행**이며, 외부 API는 사용자가 전송할 특징값을 미리 보고 명시적으로 동의한 경우에만 별도 옵션으로 제공합니다.
